<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KAVYA - Portfolio</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: 'Inter', sans-serif; 
            background: #f8fafc;
            color: #1e293b;
            line-height: 1.6;
        }
        
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        
        /* Header */
        header { 
            background: white; 
            box-shadow: 0 2px 20px rgba(0,0,0,0.08);
            position: fixed; 
            width: 100%; 
            top: 0; 
            z-index: 1000;
        }
        nav { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            padding: 1rem 0;
        }
        .logo { 
            font-size: 1.5rem; 
            font-weight: 700; 
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .nav-links { display: flex; list-style: none; gap: 2rem; }
        .nav-links a { 
            color: #64748b; 
            text-decoration: none; 
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .nav-links a:hover { color: #667eea; }
        
        /* Hero Section */
        .hero { 
            min-height: 100vh; 
            display: flex; 
            align-items: center; 
            padding-top: 80px;
            position: relative;
            background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
        }
        .hero-content { 
            display: grid; 
            grid-template-columns: 1fr 1fr; 
            gap: 8rem; 
            align-items: center;
        }
        .hero-visual { 
            position: relative;
            min-height: 400px;
        }
        .wave-pattern { 
            position: absolute; 
            top: -50px; right: -100px; 
            width: 400px; 
            height: 400px; 
            background: linear-gradient(135deg, rgba(102,126,234,0.1), rgba(118,75,162,0.1));
            border-radius: 50%;
            z-index: 1;
        }
        .hero-circle { 
            position: absolute; 
            top: 20%; left: 20%; 
            width: 200px; 
            height: 200px; 
            background: linear-gradient(135deg, #667eea, #764ba2);
            border-radius: 50%;
            z-index: 2;
            animation: pulse 3s ease-in-out infinite;
        }
        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.1); opacity: 1; }
        }
        .hero-text { z-index: 3; }
        .hero-subtitle { 
            color: #64748b; 
            font-size: 1.1rem; 
            font-weight: 400;
            margin-bottom: 1rem;
        }
        .hero-text h1 { 
            font-size: clamp(3rem, 7vw, 5rem); 
            font-weight: 700; 
            margin-bottom: 1.5rem;
            line-height: 1.1;
            background: linear-gradient(135deg, #1e293b, #334155);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .hero-text p { 
            font-size: 1.2rem; 
            color: #475569; 
            margin-bottom: 2.5rem;
            max-width: 500px;
        }
        .cta-buttons { display: flex; gap: 1.5rem; flex-wrap: wrap; }
        .btn { 
            padding: 14px 32px; 
            border-radius: 12px; 
            font-size: 1rem; 
            font-weight: 500; 
            text-decoration: none; 
            transition: all 0.3s ease;
            display: inline-flex; 
            align-items: center; 
            gap: 0.5rem;
        }
        .btn-primary { 
            background: linear-gradient(135deg, #667eea, #764ba2); 
            color: white; 
            box-shadow: 0 10px 30px rgba(102,126,234,0.3);
        }
        .btn-primary:hover { transform: translateY(-3px); box-shadow: 0 15px 40px rgba(102,126,234,0.4); }
        .btn-secondary { 
            background: white; 
            color: #64748b; 
            border: 2px solid #e2e8f0;
        }
        .btn-secondary:hover { background: #f8fafc; border-color: #cbd5e1; }
        
        /* Sections */
        section { padding: 100px 0; }
        section h2 { 
            text-align: center; 
            font-size: 2.5rem; 
            font-weight: 700; 
            margin-bottom: 4rem; 
            color: #1e293b;
        }
        
        /* About */
        #about { background: #f8fafc; }
        .about-content { 
            display: grid; 
            grid-template-columns: 1fr 2fr; 
            gap: 4rem; 
            align-items: center;
        }
        .about-text p { 
            font-size: 1.1rem; 
            color: #475569; 
            margin-bottom: 1.5rem;
        }
        
        /* Skills */
        .skills-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); 
            gap: 2rem; 
        }
        .skill-item { 
            background: white; 
            padding: 2.5rem 2rem; 
            border-radius: 16px; 
            text-align: center; 
            box-shadow: 0 10px 40px rgba(0,0,0,0.06);
            transition: all 0.3s ease;
            border: 1px solid #f1f5f9;
        }
        .skill-item:hover { 
            transform: translateY(-8px); 
            box-shadow: 0 20px 60px rgba(0,0,0,0.12);
        }
        .skill-item h3 { font-size: 1.3rem; margin-bottom: 0.5rem; color: #1e293b; }
        
        /* Projects */
        .projects-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(380px, 1fr)); 
            gap: 2.5rem; 
        }
        .project-card { 
            background: white; 
            border-radius: 20px; 
            overflow: hidden; 
            box-shadow: 0 20px 60px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            height: 320px;
        }
        .project-card:hover { 
            transform: translateY(-12px); 
            box-shadow: 0 30px 80px rgba(0,0,0,0.15);
        }
        .project-image { 
            height: 180px; 
            background: linear-gradient(135deg, #667eea, #764ba2); 
            position: relative;
        }
        .project-image::before { 
            content: attr(data-tech); 
            position: absolute; 
            top: 20px; right: 20px; 
            background: rgba(255,255,255,0.9); 
            padding: 0.3rem 0.8rem; 
            border-radius: 20px; 
            font-size: 0.8rem; 
            font-weight: 600; 
            color: #64748b;
        }
        .project-info { 
            padding: 2rem; 
        }
        .project-info h3 { 
            font-size: 1.4rem; 
            margin-bottom: 0.8rem; 
            color: #1e293b;
        }
        .project-info p { 
            color: #64748b; 
            font-size: 0.95rem; 
            margin-bottom: 1rem;
        }
        
        /* Resume */
        #resume { 
            text-align: center; 
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        }
        .resume-btn { 
            display: inline-block; 
            background: linear-gradient(135deg, #667eea, #764ba2); 
            color: white; 
            padding: 1.2rem 3rem; 
            border-radius: 50px; 
            font-size: 1.1rem; 
            font-weight: 600;
            box-shadow: 0 15px 40px rgba(102,126,234,0.3);
            transition: all 0.3s ease;
        }
        .resume-btn:hover { 
            transform: translateY(-5px); 
            box-shadow: 0 25px 60px rgba(102,126,234,0.4);
        }
        
        /* Contact */
        .contact-content { 
            display: grid; 
            grid-template-columns: 1fr 1fr; 
            gap: 4rem; 
        }
        .contact-info, .contact-form { 
            background: white; 
            padding: 3rem; 
            border-radius: 20px; 
            box-shadow: 0 20px 60px rgba(0,0,0,0.08);
        }
        .form-group { margin-bottom: 1.5rem; }
        .form-group input, .form-group textarea { 
            width: 100%; 
            padding: 1.2rem; 
            border: 2px solid #f1f5f9; 
            border-radius: 12px; 
            font-size: 1rem; 
            transition: border-color 0.3s ease;
            background: #fafbfd;
        }
        .form-group input:focus, .form-group textarea:focus { 
            outline: none; 
            border-color: #667eea; 
        }
        .form-group button { 
            width: 100%; 
            background: linear-gradient(135deg, #667eea, #764ba2); 
            color: white; 
            border: none; 
            padding: 1.2rem; 
            border-radius: 12px; 
            font-size: 1.1rem; 
            font-weight: 600;
            cursor: pointer;
        }
        
        /* Footer */
        footer { 
            background: #1e293b; 
            color: #94a3b8; 
            text-align: center; 
            padding: 3rem 0;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .hero-content, .about-content, .contact-content { grid-template-columns: 1fr; gap: 2rem; text-align: center; }
            .nav-links { gap: 1rem; }
            .cta-buttons { justify-content: center; }
            section { padding: 60px 0; }
            .projects-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav class="container">
            <div class="logo">Portfolio</div>
            <ul class="nav-links">
                <li><a href="#about">About</a></li>
                <li><a href="#skills">Skills</a></li>
                <li><a href="#projects">Projects</a></li>
                <li><a href="#resume">Resume</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content container">
            <div class="hero-visual">
                <div class="wave-pattern"></div>
                <div class="hero-circle"></div>
            </div>
            <div class="hero-text">
                <div class="hero-subtitle">Creative Web Developer</div>
                <h1>KAVYA</h1>
                <p>Building responsive websites with modern HTML, CSS & JavaScript. Currently completing CodSoft internship from Chennai, Tamil Nadu.</p>
                <div class="cta-buttons">
                    <a href="#projects" class="btn btn-primary">View Projects →</a>
                    <a href="#contact" class="btn btn-secondary">Contact Me</a>
                </div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about">
        <div class="container">
            <h2>About Me</h2>
            <div class="about-content">
                <div></div>
                <div class="about-text">
                    <p>Passionate web developer from Chennai specializing in responsive web design. Currently working on CodSoft internship projects including portfolios, landing pages, and interactive calculators.</p>
                    <p>I focus on clean code, modern design principles, and pixel-perfect layouts that work beautifully across all devices.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section id="skills">
        <div class="container">
            <h2>Skills</h2>
            <div class="skills-grid">
                <div class="skill-item">
                    <h3>HTML5 & CSS3</h3>
                    <p>Semantic markup and modern styling with Flexbox, Grid, and animations</p>
                </div>
                <div class="skill-item">
                    <h3>JavaScript</h3>
                    <p>Interactive features, DOM manipulation, and event handling</p>
                </div>
                <div class="skill-item">
                    <h3>Responsive Design</h3>
                    <p>Mobile-first approach with fluid layouts and breakpoints</p>
                </div>
                <div class="skill-item">
                    <h3>UI/UX Principles</h3>
                    <p>User-centered design with focus on accessibility</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Projects Section -->
    <section id="projects">
        <div class="container">
            <h2>Projects</h2>
            <div class="projects-grid">
                <div class="project-card">
                    <div class="project-image" data-tech="HTML/CSS"></div>
                    <div class="project-info">
                        <h3>Responsive Landing Page</h3>
                        <p>Modern landing page for product showcase featuring smooth scroll animations, CSS Grid layout system, and mobile-first responsive design. Built with pure HTML5 and CSS3 for optimal performance.</p>
                    </div>
                </div>
                <div class="project-card">
                    <div class="project-image" data-tech="HTML/CSS/JS"></div>
                    <div class="project-info">
                        <h3>Interactive Calculator</h3>
                        <p>Fully functional calculator with CSS Grid interface, JavaScript event listeners for real-time calculations, keyboard support, and modern neumorphic design. Handles all basic arithmetic operations.</p>
                    </div>
                </div>
                <div class="project-card">
                    <div class="project-image" data-tech="HTML/CSS"></div>
                    <div class="project-info">
                        <h3>Personal Portfolio</h3>
                        <p>This portfolio website showcasing clean code practices, glassmorphism effects, smooth animations, and perfect responsive design across all devices. Built for CodSoft internship Task 1.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Resume Section -->
    <section id="resume">
        <div class="container">
            <h2>Resume</h2>
            <a href="your-resume.pdf" class="resume-btn" download>Download Resume</a>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact">
        <div class="container">
            <h2>Contact</h2>
            <div class="contact-content">
                <div class="contact-info">
                    <h3>Get In Touch</h3>
                    <p><strong>Email:</strong><br>your.email@example.com</p>
                    <p><strong>Phone:</strong><br>+91 98765 43210</p>
                    <p><strong>Location:</strong><br>Chennai, Tamil Nadu</p>
                    <p><strong>LinkedIn:</strong><br>linkedin.com/in/yourprofile</p>
                    <p><strong>GitHub:</strong><br>github.com/yourusername</p>
                </div>
                <div class="contact-form">
                    <form>
                        <div class="form-group">
                            <input type="text" placeholder="Your Name" required>
                        </div>
                        <div class="form-group">
                            <input type="email" placeholder="Your Email" required>
                        </div>
                        <div class="form-group">
                            <input type="text" placeholder="Subject">
                        </div>
                        <div class="form-group">
                            <textarea rows="5" placeholder="Your Message" required></textarea>
                        </div>
                        <div class="form-group">
                            <button type="submit">Send Message</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <p>&copy; 2025 Your Name. All rights reserved | Made for CodSoft Web Development Internship</p>
        </div>
    </footer>
</body>
</html>
